---
"TestAgent": patch
---

Support using VS Code's integrated browser as an agent tool, so the agent can open, read, screenshot and navigate pages without a separate Playwright install. Enable or disable it from Settings → Browser. It is enabled by default, and enabling Playwright browser automation turns it off, since only one browser backend can be active at a time.
