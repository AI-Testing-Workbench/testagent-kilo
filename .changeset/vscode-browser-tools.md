---
"TestAgent": patch
---

Support using VS Code's integrated browser as an agent tool, so the agent can open, read, screenshot and navigate pages without a separate Playwright install. Enable it from Settings → Browser. It is off by default, and turning either it or Playwright browser automation on turns the other off, since only one browser backend can be active at a time.
