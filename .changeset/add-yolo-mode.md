---
"TestAgent": minor
"@kilocode/cli": minor
---

Support YOLO mode for sessions: enable it from the prompt input to run unattended — all permission rules are bypassed, the question tool becomes unavailable, and the agent decides on its own without waiting for approval.
