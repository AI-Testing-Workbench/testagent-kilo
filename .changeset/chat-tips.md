---
"kilo-code": minor
---

Add a chat tip above the chat input, mirroring the VS Code Copilot tip widget, with markdown content, command links, and previous/next navigation. Tips are fetched once at startup from a built-in remote endpoint and fail silently.
